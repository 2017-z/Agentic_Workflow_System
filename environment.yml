name: agentic_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - pydantic>=2.0.0
      - aiohttp>=3.9.0
      - python-dotenv
      - openai>=1.0.0
      - gradio>=4.0.0